name: Sync Release to Alist

on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  upload:
    runs-on: ubuntu-latest
    env:
      ALIST_USER: ${{ secrets.ALIST_USER }}
      ALIST_PASS: ${{ secrets.ALIST_PASSWORD }}
    steps:
      - name: Get Release Assets
        id: get_assets
        run: |
          RELEASE_ID=${{ github.event.release.id || 'latest' }}
          echo "assets<<EOF" >> $GITHUB_OUTPUT
          curl -s https://api.github.com/repos/${{ github.repository }}/releases/$RELEASE_ID | jq -r '.assets[].browser_download_url' >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Download and Upload to Alist
        run: |
          DRIVES=("Aliyun" "Baidu")
          REPO_NAME=$(echo "${{ github.repository }}" | cut -d'/' -f2)
          ALIST_URL="http://115.29.162.18:5244/dav"

          # 检查 Secrets
          if [ -z "$ALIST_USER" ] || [ -z "$ALIST_PASS" ]; then
            echo "❌ Error: ALIST_USER or ALIST_PASSWORD secret is missing!"
            exit 1
          fi

          for url in ${{ steps.get_assets.outputs.assets }}; do
            filename=$(basename $url)
            echo "------------------------------------------------"
            echo "⬇️ 正在下载 GitHub 文件: $filename"
            
            # 1. 先下载到本地 (解决百度云 WebDAV 405 流式上传兼容性问题)
            curl -L "$url" -o "$filename"

            for drive in "${DRIVES[@]}"; do
              echo "🚀 正在同步到 $drive: $drive/$REPO_NAME/$filename"
              
              # 2. 上传本地文件
              # 使用 --fail 确保 405 时立刻报错
              # 百度云 405 通常是由于父目录未挂载成功或驱动不支持 PUT
              curl -v --fail \
                --user "$ALIST_USER:$ALIST_PASS" \
                -T "$filename" \
                "$ALIST_URL/$drive/$REPO_NAME/$filename"
              
              if [ $? -eq 0 ]; then
                echo "✅ $drive 同步完成"
              else
                echo "❌ $drive 同步失败"
                exit 1
              fi
            done
            
            # 清理本地缓存
            rm "$filename"
          done
